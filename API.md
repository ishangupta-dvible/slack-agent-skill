# Slack Agent Skill API

This document describes the API and internal functions provided by the Slack Agent Skill.

## Capabilities

- Read and write to specific Slack channels.
- Authorize as a Bot user via OAuth2.

## Endpoints

There are no exposed HTTP endpoints. This skill integrates natively with the Agent architecture and the Slack WebSocket or REST API via the `slack.com` declared domain.
